# Supabase PostgreSQL connection string for Railway (Session Pooler, IPv4 compatible)
# Get this exact value from Supabase Dashboard > Connect > Session pooler
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres

# Resend email API key (https://resend.com)
RESEND_API_KEY=re_...

# The "from" address for confirmation emails (must be a verified domain in Resend)
FROM_EMAIL=orders@lokucaters.com

# Comma-separated recipients for new order, feedback, and catering alerts
NOTIFICATION_EMAILS=owner@example.com,manager@example.com

# Optional reply-to address for customer responses
REPLY_TO_EMAIL=you@example.com

# Toggle email sending without changing order behavior
EMAIL_ENABLED=true
EMAIL_REQUEST_TIMEOUT_SECONDS=15

# Your deployed frontend URL (used for CORS)
FRONTEND_URL=https://your-frontend.railway.app

# Supabase JWT secret (legacy HS256 projects only, also used by DEV_MODE local login flow)
# Modern Supabase projects with RS256/ES256 do not require this for admin token verification
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# Canonical project URL used to pin JWT issuer and JWKS lookups
SUPABASE_URL=https://your-project-ref.supabase.co

# Comma-separated allowlist for production admin accounts. When this is empty,
# the admin API fails closed unless the explicit override below is enabled.
ADMIN_EMAILS=admin@example.com
ALLOW_ALL_AUTHENTICATED_ADMINS=false

# Railway contributes one trusted reverse proxy hop to X-Forwarded-For.
# Keep this at 0 when the backend is directly exposed without a trusted proxy.
RATE_LIMIT_TRUSTED_PROXY_HOPS=1

# In-process limiter and maximum decoded request size in bytes
RATE_LIMIT_ENABLED=true
MAX_REQUEST_BODY_BYTES=2097152
