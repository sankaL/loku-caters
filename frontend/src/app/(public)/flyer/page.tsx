import FlyerClient from "./FlyerClient";

export const metadata = {
    title: "Catering Flyer | Loku Caters",
    description:
        "Browse our latest appetizer and tray offerings with pricing. Rolls, pastries, patties, cutlets, curry trays, and more.",
};

export default function FlyerPage() {
    return <FlyerClient />;
}
